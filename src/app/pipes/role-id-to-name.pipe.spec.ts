import { RoleIdToNamePipe } from './role-id-to-name.pipe';

describe('RoleIdToNamePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleIdToNamePipe();
    expect(pipe).toBeTruthy();
  });
});
